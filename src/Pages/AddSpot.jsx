import { Helmet } from 'react-helmet';

const AddSpot = () => {
  console.log(name);
  return (
    <div>
      <Helmet>
        <title>Add Tourist Spot | Tracker</title>
      </Helmet>
      Add spot
    </div>
  );
};

export default AddSpot;
